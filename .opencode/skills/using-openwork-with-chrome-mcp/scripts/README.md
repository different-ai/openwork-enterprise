# Scripts

This skill currently uses Chrome MCP tools directly and does not require scripts.

If future flows need reusable automation, add scripts here and document any required environment variables in `../.env.example`.
